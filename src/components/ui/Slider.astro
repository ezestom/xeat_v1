---
interface Slider {
	text: string;
	text2: string;
	style: string;
}
const { text, text2, style } = Astro.props;
---

<article class="text-5xl md:text-8xl overflow-hidden">
	<div class="slider-container">
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
		<p class="mx-4 font-black uppercase flex text-nowrap">
			<span class="empty-text">{text}<span class=""> ● </span></span>
			{text2}
		</p>
		<span class=""> ● </span>
	</div>
</article>

<style>
	div {
		display: flex;
		align-items: center;
		justify-content: center;
		width: 100%;
		animation: slide 75s linear infinite;
	}

	@keyframes slide {
		0% {
			transform: translateX(0);
		}
		100% {
			transform: translateX(-100%);
		}
	}

	.empty-text {
		/* color: rgba(0, 0, 0, 0); 
		-webkit-text-stroke: 1px var(--black); 
		text-stroke-width: 1px;  */
	}
</style>
