---
const response = await fetch(
	"https://us-east4.gcp.data.mongodb-api.com/app/home-ybjtf/endpoint/210"
);
const data = await response.json();
---

<article
	class="flex flex-col md:flex-row border border-black max-h-full md:max-h-[50vh]">
	<aside class="flex justify-around items-center w-full bg-[--black]">
		<div
			class="w-full md:max-w-2/3 text-center text-[--white3] p-10 md:p-20">
			<h3 class="text-3xl md:text-5xl font-bold uppercase pb-4">
				The burger vilas | since 2020
			</h3>
			<p class="text-xl">
				Nuestro mayor objetivo es que la pasión por lo que hacemos se
				refleje en cada pedido.
			</p>
		</div>
		<img
			src={data.logo_url}
			alt="logo burger"
			class="pr-2 md:pr-10 w-1/2 md:w-1/3 hidden md:block"
		/>
	</aside>
	<div class="w-full md:w-1/3 overflow-hidden">
		<img
			src={data.slider_categorias[0].imagen}
			alt="burger image"
			class="scale-[1.75]"
		/>
	</div>
</article>
<article
	class="flex flex-col md:flex-row border border-black max-h-full md:max-h-[50vh]">
	<aside
		class="flex flex-col-reverse md:flex-row text-center items-center w-full bg-[--white3] overflow-hidden">
		<div class="w-full md:w-2/3 overflow-hidden border-r border-black">
			<img
				src={data.slider_productos[3].imagen}
				alt="burger image"
				class="max-h-[400px] md:max-h-full scale-[1.5] md:scale-[1] object-center object-cover"
			/>
		</div>
		<div class="w-full p-10 md:p-20">
			<h3 class="text-3xl md:text-6xl font-semibold">
				En The Burgers Vilas
			</h3>
			<p class="text-2xl md:text-3xl">
				Hacemos hamburguesas con ingredientes frescos y de calidad.
			</p>
		</div>
	</aside>
</article>

<style>
	#background {
		background-image: url("https://habitue-img.s3.sa-east-1.amazonaws.com/imagenes/1648827746.blob");
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;

		height: 100%;
		z-index: 10;
	}
	img {
		filter: drop-shadow(0 0 1rem rgba(255, 255, 255, 0.5));
	}
	@media (max-width: 1150px) {
		section {
			flex-direction: column;
		}
		article {
			width: 100%;
		}
	}
</style>
