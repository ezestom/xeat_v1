---
import { Image } from "astro:assets";
import hotdog from "../../img/hotdog.png";
import fries from "../../img/cheddarBacon.png";
const response = await fetch(
	"https://us-east4.gcp.data.mongodb-api.com/app/home-ybjtf/endpoint/210"
);
const data = await response.json();
---

<article class="flex flex-col md:flex-row max-h-full md:max-h-[50vh]">
	<aside class="flex justify-around items-center w-full">
		<div class="w-full md:max-w-1/2 text-center text-black p-10 md:p-20">
			<h3 class="text-3xl md:text-5xl font-bold uppercase pb-4">
				The burger vilas | since 2020
			</h3>
			<p class="text-xl">
				Nuestro mayor objetivo es que la pasión por lo que hacemos se
				refleje en cada pedido.
			</p>
		</div>
	</aside>
	<div class="w-full">
		<Image class="" src={hotdog} alt="hotdog image" />
		<!--
			<img
			id="img-burger"
			src={data.slider_categorias[0].imagen}
			alt="burger image"
			class="scale-[1.75]"
		/> -->
	</div>
</article>
<article class="flex flex-col md:flex-row max-h-full md:max-h-[50vh]">
	<aside
		class="flex flex-col-reverse md:flex-row text-center items-center w-full overflow-hidden">
		<div class="w-full overflow-hidden">
			<Image class="" src={fries} alt="fries image" />
			<!-- <img
				id="img-burger"
				src={data.slider_productos[3].imagen}
				alt="burger image"
				class="max-h-[400px] md:max-h-full scale-[1.5] md:scale-[1] object-center object-cover"
			/> -->
		</div>
		<div class="w-full p-10 md:p-20">
			<h3 class="text-3xl md:text-6xl font-semibold">
				En The Burgers Vilas
			</h3>
			<p class="text-2xl md:text-3xl">
				Hacemos hamburguesas con ingredientes frescos y de calidad.
			</p>
		</div>
	</aside>
</article>

<style>
	#background {
		background-image: url("https://habitue-img.s3.sa-east-1.amazonaws.com/imagenes/1648827746.blob");
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;
		height: 100%;
		z-index: 10;
	}
	#img-burger {
		/* create a mask rounded to centre transparent */
		mask-image: radial-gradient(white 10%, transparent 50%);
	}
	img {
		filter: drop-shadow(0 0 1rem rgba(255, 255, 255, 0.5));
	}
	@media (max-width: 1150px) {
		section {
			flex-direction: column;
		}
		article {
			width: 100%;
		}
	}
</style>
